plugins {
    id "com.android.application"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.ata.atawhatsapp"
    compileSdk = flutter.compileSdkVersion

    defaultConfig {
        applicationId = "com.ata.atawhatsapp"
        minSdk = 23
        targetSdk = flutter.targetSdkVersion
        versionCode = 1
        versionName = "1.0.0"
    }
}

flutter {
    source = "../.."
}
